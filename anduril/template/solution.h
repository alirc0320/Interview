#pragma once

/* Declare your function(s) here. Pure C — no C++ headers, no classes,
 * no exceptions/optional/templates. Prefer fixed-width types (stdint.h)
 * and bool (stdbool.h) the way you would on a real embedded target. */

int solve(int x);
