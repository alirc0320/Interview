// Combined practice program for LearnCpp chapters 2 (C++ Basics: Functions
// and Files) and 3 (Debugging C++ Programs).
// One program covering ideas from both chapters, 20-40 lines, at
// least 5 assert() checks.
//
// Compile:
//   clang++ -std=c++20 -Wall -Wextra -Wpedantic -Wconversion \
//     -g -O0 -fsanitize=address,undefined practice.cpp -o practice
// Run:
//   ./practice

#include <cassert>
#include <iostream>

int main()
{
    // TODO: Write at least three small functions (e.g. one that returns a
    // value, one void function, one that takes multiple parameters) with
    // forward declarations above main() and definitions below it, and call
    // them from main(). Then deliberately introduce one logic bug (not a
    // compile error) into one of the functions, find it yourself by
    // reasoning through the code or adding temporary debug prints, fix it,
    // and leave a one-line comment describing what the bug was and how you
    // found it.

    // TODO: at least 5 assert() checks exercising each function's return
    // value/behavior across a few different inputs.

    std::cout << "ok\n";
    return 0;
}
